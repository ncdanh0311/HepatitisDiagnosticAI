# Shared Resources
This directory contains code and data shared across multiple microservices.

- `models/`: ML model artifacts (.pkl files).
- `data/`: Raw and processed datasets.
- `schemas/`: (Future) Shared Pydantic/TypeScript models.
